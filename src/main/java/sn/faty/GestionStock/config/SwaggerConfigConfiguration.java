package sn.faty.GestionStock.config;

/*
@Configuration
@EnableSwagger2
public class SwaggerConfigConfiguration {

    public Docket api()
    {
        return  new Docket(DocumentationType.SWAGGER_2)
                .apiInfo(
                   new  ApiInfoBuilder()
                           .description("Documentation API REST")
                           .title("Gestion stock")
                           .build()
                )
                .groupName("API REST ")
                .select()
                .apis(RequestHandlerSelectors.basePackage("sn.faty.GestionStock"))
                .paths(PathSelectors.ant(APP_ROOT + "/**"))
                .build();
    }


 */

